<%= form_for(@cultural_background) do |f| %>
  <% if @cultural_background.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cultural_background.errors.count, "error") %> prohibited this cultural_background from being saved:</h2>

      <ul>
      <% @cultural_background.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :class => "tinymce", :rows => 20, :cols => 50 %>
    <%= tinymce %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.datetime_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :end_date %><br />
    <%= f.datetime_select :end_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
